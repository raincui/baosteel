<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:mm="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <com.android.yl.baowu.baseui.customview.PagerSlidingTabStrip
            android:id="@+id/steel_tabs"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            mm:pstsIndicatorColor="@android:color/transparent"
            mm:pstsIndicatorHeight="0dp"
            mm:pstsTabBackground="@android:color/transparent"
            mm:pstsUnderlineColor="@android:color/transparent"
            mm:pstsUnderlineHeight="0dp"

            />

        <ImageView
            android:id="@+id/btn_look"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:paddingLeft="10dp"
            android:paddingRight="5dp"
            android:src="@drawable/tianjia" />
    </LinearLayout>

    <android.support.v4.view.ViewPager
        android:id="@+id/steel_pager"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />
</LinearLayout>
